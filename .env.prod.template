# Production Environment Variables Template
# Copy this file to .env.prod and update with your production values

# Database Configuration
POSTGRES_DB=jarvisdb_prod
POSTGRES_USER=jarvis_prod
POSTGRES_PASSWORD=your_secure_password_here

# Application Configuration (optional overrides)
# SPRING_PROFILES_ACTIVE=prod
# SERVER_PORT=8080

# JVM Memory Configuration (optional - defaults set in docker-compose)
# JAVA_OPTS=-Xms512m -Xmx1024m -XX:+UseG1GC

# SSL Configuration (if using HTTPS)
# SSL_CERT_PATH=/path/to/certificate
# SSL_KEY_PATH=/path/to/private/key